import styled from 'styled-components';

export const Container = styled.div`
    h1 {
        color: var(--title);
        font-size: 2.5rem;
    }

    p {
        width: 30%;
        color: var(--text);
        line-height: 1.5rem;
        margin: 1.5rem 0 4rem;
    }
`